spring:
  application:
    name: vmall-user-manager
  datasource:
    url: jdbc:mysql://localhost:3306/vmall_user
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8082

eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka


jwt:
  key: WPjV#?v"9.xUQ[S@&87)(=

